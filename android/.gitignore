# Android generated files
*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
*.apk
*.aab

# google-services.json contains API keys — never commit this
app/google-services.json

# gradle-wrapper.jar is intentionally tracked so CI builds work without Gradle pre-installed
